/**
 * Empty module replacement for server-only modules in client builds
 * This file is used by webpack to replace server-only imports in client code
 */

// This module should never be imported directly
// It's only used as a webpack replacement for server-only modules

export {};

